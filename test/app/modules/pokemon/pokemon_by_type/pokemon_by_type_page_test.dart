import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PokemonByTypePage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PokemonByTypePage(title: 'PokemonByType')));
    //  final titleFinder = find.text('PokemonByType');
    //  expect(titleFinder, findsOneWidget);
  });
}
