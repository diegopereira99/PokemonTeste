import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('PokemonDetailPage has title', (tester) async {
    //  await tester.pumpWidget(buildTestableWidget(PokemonDetailPage(title: 'PokemonDetail')));
    //  final titleFinder = find.text('PokemonDetail');
    //  expect(titleFinder, findsOneWidget);
  });
}
